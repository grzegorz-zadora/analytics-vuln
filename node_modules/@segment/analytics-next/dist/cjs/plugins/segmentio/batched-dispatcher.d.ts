declare type BatchingConfig = {
    size?: number;
    timeout?: number;
};
export default function batch(apiHost: string, config?: BatchingConfig): {
    dispatch: (_url: string, body: object) => Promise<unknown>;
};
export {};
