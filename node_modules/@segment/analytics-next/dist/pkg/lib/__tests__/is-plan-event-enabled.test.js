import { isPlanEventEnabled } from '../is-plan-event-enabled';
describe('isPlanEventEnabled', function () {
    it('handles tracking plans that allow unplanned events', function () {
        var plan = {
            __default: { enabled: true, integrations: {} },
        };
        expect(isPlanEventEnabled(plan, { enabled: true, integrations: {} })).toBe(true);
        expect(isPlanEventEnabled(plan, { enabled: false, integrations: {} })).toBe(false);
        expect(isPlanEventEnabled(plan, undefined)).toBe(true);
    });
    it('handles tracking plans that disallow unplanned events', function () {
        var plan = {
            __default: { enabled: false, integrations: {} },
        };
        expect(isPlanEventEnabled(plan, { enabled: true, integrations: {} })).toBe(true);
        expect(isPlanEventEnabled(plan, { enabled: false, integrations: {} })).toBe(false);
        expect(isPlanEventEnabled(plan, undefined)).toBe(false);
    });
    it('allows event when tracking plan does not exist', function () {
        expect(isPlanEventEnabled(undefined, undefined)).toBe(true);
    });
});
//# sourceMappingURL=is-plan-event-enabled.test.js.map